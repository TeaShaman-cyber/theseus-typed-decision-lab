# Contract boundary

The canonical shared typed System-1 decision contract is owned by
`TeaShaman-cyber/theseus-needle-lab#55` until that issue produces a versioned
artifact.

This repository MUST consume an exact accepted/versioned contract instead of
silently creating a semantic fork.

The bootstrap fixture under `fixtures/smoke/` is SemIf-native runtime input only.
It is not the Theseus cross-model benchmark contract.
